# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Phase 5 (data layer) complete. Editor home/sidebar run on real data end to end. Workspace route shell (`/editor/[roomId]`) now exists with server-side access checks — no canvas/Liveblocks/AI/sharing logic yet.

## Current Goal

- 09-share-dialog complete. Next: real canvas logic (Liveblocks room provider + React Flow) inside `WorkspaceShell`, replacing the placeholder center panel.

## Completed

- 01-design-system: shadcn/ui installed (Base UI style, base-nova), Button/Card/Dialog/Input/Tabs/Textarea/ScrollArea added, lucide-react installed, lib/utils.ts cn() helper created, globals.css wired to dark-only palette from context/ui-context.md (no light mode block, `dark` class always on `<html>`). Verified via `tsc --noEmit`, `next build`, and a rendered smoke route (removed after verification).
- 02-editor-chrome: `components/editor/editor-navbar.tsx` (fixed top navbar, left/center/right sections, sidebar toggle button switching `PanelLeftOpen`/`PanelLeftClose`, dark bg-surface with bottom border) and `components/editor/project-sidebar.tsx` (fixed floating overlay sliding in from left via translate transform, doesn't push content, `Projects` header + close button, `Tabs` with My Projects/Shared both showing empty placeholder text, full-width `New Project` button with `Plus` icon) built per context/feature-specs/02-editor-chrome.md. Dialog pattern requirement already satisfied by existing components/ui/dialog.tsx (title/description/footer, uses globals.css tokens via popover/muted mappings) — no new dialog built. Verified via `tsc --noEmit`, `eslint` (npm run lint), `next build`, and a Playwright smoke test against a temporary route (screenshots of closed/open sidebar states, no console errors; route and temp playwright install removed after verification).
- 03-auth: `@clerk/ui` installed for the `dark` base theme (`@clerk/ui/themes`). `app/layout.tsx` wraps the app in `ClerkProvider` with `appearance={{ theme: dark, variables: {...} }}`, all variable values pulled from the existing CSS custom properties in globals.css (no hardcoded colors). `proxy.ts` created at the project root (this Next.js version renamed `middleware.ts` to `proxy.ts` — confirmed via node_modules/next/dist/docs) using `clerkMiddleware` + `createRouteMatcher` from `@clerk/nextjs/server`; public routes are read from `NEXT_PUBLIC_CLERK_SIGN_IN_URL`/`NEXT_PUBLIC_CLERK_SIGN_UP_URL` (added to `.env.local`, they didn't exist before), everything else calls `auth.protect()`. `app/sign-in/[[...sign-in]]/page.tsx` and `app/sign-up/[[...sign-up]]/page.tsx` render Clerk's `SignIn`/`SignUp` inside a shared `components/auth/auth-layout.tsx` (true 50/50 split on `lg:` via `w-1/2`/`w-1/2`, left panel on `bg-surface` to read as a distinct panel against the `bg-base` right side, form-only below `lg:`, no gradients/hero/feature cards per spec). `app/page.tsx` is now an async server component using `auth()` to redirect signed-in users to `/editor` and everyone else to `/sign-in`. `UserButton` added to the navbar's right section in `components/editor/editor-navbar.tsx`. Verified via `tsc --noEmit`, `eslint`, `next build` (proxy correctly registered as "Proxy (Middleware)" in build output, not middleware), curl checks that `/`, `/editor` redirect unauthenticated requests to `/sign-in` while `/sign-in` itself returns 200, and a temporary Playwright smoke test screenshotting sign-in/sign-up at desktop and mobile widths (no console errors; script and Playwright browser install removed after verification).
- 04-project-dialogs: `lib/projects.ts` holds the `Project`/`ProjectRole` types, a `slugify()` helper, and seed `mockProjects` (two `owner` projects, one `collaborator` project — no API/persistence). `hooks/use-project-dialogs.ts` is the single state owner: which dialog is open (`create`/`rename`/`delete`/`null`), the active project, the name/slug form field, a simulated `isLoading` (400ms delay) for create/rename/delete, and the mutating submit handlers that update the local `projects` array in place (create appends, rename edits in place with a re-slugified slug, delete filters it out). `components/editor/project-dialogs-context.tsx` calls the hook once, renders the three dialog components declaratively from its state, and exposes only `{ projects, openCreateDialog, openRenameDialog, openDeleteDialog }` via React context — this is what lets both the editor-home button and the sidebar rows trigger dialogs without prop-drilling through the `app/editor` route boundary. `components/editor/editor-chrome.tsx` wraps its children in `ProjectDialogsProvider`. `components/editor/editor-home.tsx` (client) renders the heading/description/`New Project` button per spec, uncarded; `app/editor/page.tsx` stays a server component and just renders it. Dialogs live in `components/editor/dialogs/`: `create-project-dialog.tsx` (name input + live `/slug` preview text that updates on every keystroke via `slugify(name)`), `rename-project-dialog.tsx` (prefilled name, description names the current project, input auto-focused via base-ui's `initialFocus` ref prop on `DialogContent`, Enter submits via wrapping `<form>`), `delete-project-dialog.tsx` (no input, `variant="destructive"` confirm button). `components/editor/project-sidebar.tsx` renders `mockProjects` split by `role` into the My Projects/Shared tabs; each owned row gets a `DropdownMenu` (new `components/ui/dropdown-menu.tsx`, added via `npx shadcn add dropdown-menu`) with Rename/Delete items — shared/collaborator rows render plain text with no menu, satisfying "hide actions for shared projects." Added a `lg:hidden` backdrop scrim (`fixed inset-0 bg-black/50`, opacity-animated) behind the sidebar that closes it on click, for the mobile tap-outside-to-close requirement. Verified via `tsc --noEmit`, `eslint`, `next build`, and a temporary Playwright smoke test against a temporary public `/dev-preview` route (added to `proxy.ts`'s public matcher only for the duration of the test, then reverted) that exercised: create dialog + live slug preview updating as typed, opening the sidebar, hovering a row to reveal its menu, renaming a project (confirmed the input receives focus programmatically, Enter submits), deleting a project (mock list updates, row disappears), confirming the Shared tab shows no row menu, and the mobile backdrop closing the sidebar on an outside tap. No console errors in any step; route, script, and the temporarily-installed Playwright package were removed after verification and `.next` was cleared to drop stale route type references.

- 05-prisma: `prisma/models/project.prisma` adds `ProjectStatus` enum (`DRAFT`/`ARCHIVED`), `Project` (cuid `id`, `ownerId` for the Clerk user, `name`, optional `description`, `status` defaulting to `DRAFT`, optional `canvasJsonPath`, `createdAt`/`updatedAt`, indexes on `ownerId` and `createdAt`), and `ProjectCollaborator` (cuid `id`, `projectId` with `onDelete: Cascade`, `email`, `createdAt`, unique `[projectId, email]`, indexes on `email` and `[projectId, createdAt]`). `lib/prisma.ts` is a cached singleton on `globalThis` that branches on `DATABASE_URL`: `prisma+postgres://` prefix uses `new PrismaClient({ accelerateUrl })`, anything else uses `new PrismaClient({ adapter: new PrismaPg({ connectionString }) })` — both are built into `@prisma/client` 7.9.0's constructor types directly (no `@prisma/extension-accelerate` package needed/installed). Ran `npx prisma migrate dev --name init_project_models` against the `DATABASE_URL` in `.env.local` (Prisma Postgres direct TCP connection, `db.prisma.io`) then `npx prisma generate`. Verified via `tsc --noEmit`, `eslint`, and `next build` (all clean).
- 06-project-apis: `app/api/projects/route.ts` (`GET` lists `prisma.project.findMany({ where: { ownerId: userId } })` ordered by `createdAt desc`; `POST` creates with `ownerId` from Clerk `auth()`, `name` defaulted server-side to `"Untitled Project"` when missing/blank) and `app/api/projects/[projectId]/route.ts` (`PATCH` renames, `DELETE` removes — both `findUnique` first, `404` if missing, `403` if `project.ownerId !== userId`, using the typed `RouteContext<'/api/projects/[projectId]'>` global helper for `ctx.params`). All four handlers call `await auth()` from `@clerk/nextjs/server` directly and return `401` JSON themselves rather than relying on middleware. No `slug` field — the API mirrors the actual `Project` Prisma model, not `lib/projects.ts`'s mock shape (which has `slug`, doesn't exist in the schema). `lib/projects.ts`'s mock CRUD is untouched — UI wiring is a separate follow-up, per spec ("keep this backend-only, do not wire the UI yet"). Backend-only: no UI, `lib/projects.ts`, or dialog/hook files were touched.

- 07-wire-editor-home: `app/editor/layout.tsx` (not `app/editor/page.tsx` — the layout is what actually renders `ProjectSidebar`, since it wraps every `/editor/*` route) is now an async server component: calls Clerk's `auth()`/`currentUser()`, then `prisma.project.findMany` for owned (`ownerId: userId`) and shared (`collaborators: { some: { email } }`, matched on the user's primary email since `ProjectCollaborator` has no Clerk user id, only `email`) projects in parallel, and passes both arrays as props through `EditorChrome` → `ProjectDialogsProvider` → context, replacing the old single mock `projects` array split by a `role` field. `lib/projects.ts` no longer defines its own `Project`/`ProjectRole` types or `mockProjects` — `Project` is now re-exported directly from `@/app/generated/prisma/client` (the real Prisma model shape: `id`, `ownerId`, `name`, `description`, `status`, `canvasJsonPath`, `createdAt`, `updatedAt`, no `slug`). `slugify()` is unchanged; added `generateRoomSuffix()` (short base36 string) alongside it. `hooks/use-project-dialogs.ts` (mock-array mutator) is deleted, replaced by `hooks/use-project-actions.ts` exporting `useProjectActions()` per the spec's naming — same dialog-state shape as before (`dialogType`/`activeProject`/`name`/`isLoading`) plus a `roomId` field (`slugify(name)-suffix`, suffix regenerated each time `openCreateDialog` runs so it doesn't drift across renders), and the three submit handlers now call the real API (`fetch` to `/api/projects`, `/api/projects/[id]` PATCH/DELETE) instead of mutating local state: create `POST`s `{ id: roomId, name }` and on success `router.push`es to `/editor/${project.id}`; rename `PATCH`s and calls `router.refresh()`; delete `DELETE`s, then compares `usePathname()` against `/editor/${activeProject.id}` to decide between `router.push("/editor")` (active workspace) and `router.refresh()` (otherwise). `app/api/projects/route.ts`'s `POST` handler now accepts an optional client-supplied `id` in the body (validated against `/^[a-z0-9-]{1,80}$/`, falls back to Prisma's default `cuid()` if absent/invalid) so the client-generated room id becomes the actual `Project.id` — see the architecture decision below for why. `components/editor/project-sidebar.tsx` now reads `ownedProjects`/`sharedProjects` straight from context instead of filtering a combined list by `role`. `create-project-dialog.tsx`'s slug-preview prop was renamed `slug` → `roomId` to match. Verified via `tsc --noEmit`, `eslint`, `next build`, and an end-to-end Playwright smoke test (temporarily installed `playwright` + `@clerk/testing`, both removed after): used `@clerk/testing/playwright`'s `clerkSetup()` + `setupClerkTestingToken()` to bypass the Cloudflare Turnstile bot-protection challenge on real sign-in (a plain Playwright click could not reliably solve it), created a throwaway Clerk user via `@clerk/backend`, signed in with `clerk.signIn({ emailAddress })` (ticket strategy, no UI form needed), then against the real dev Postgres DB: confirmed the sidebar starts empty, created a project from the home button (room-id preview matched what was actually sent), confirmed the URL navigated to `/editor/{roomId}`, went back to `/editor` and confirmed the new project appears in the sidebar, renamed it (dialog pre-filled the old name, list updated after `router.refresh()`), deleted it (list emptied after `router.refresh()`), and deleted the throwaway Clerk user at the end. No console errors other than expected 404s for `/editor/[projectId]` itself, which is out of scope for this feature (see Next Up).

- 08-editor-workspace-shell: `lib/project-access.ts` exports `getCurrentIdentity()` (Clerk `auth()` + `currentUser()`, returns `{ userId, email }` or `null`) and `getProjectForIdentity(projectId, identity)` (owner check by `ownerId`, collaborator check via `prisma.projectCollaborator.findUnique` on the `[projectId, email]` unique key; returns the `Project` or `null` for missing/unauthorized — same `null` for both so the caller can't distinguish "doesn't exist" from "not yours"). `app/editor/[roomId]/page.tsx` is an async server component: redirects to `/sign-in` when `getCurrentIdentity()` is `null`, renders `AccessDenied` when `getProjectForIdentity` is `null`, otherwise renders `WorkspaceShell`. `components/editor/access-denied.tsx` is a small server component (lock icon in a rounded `bg-elevated` tile, message, `Button` with `render={<Link href="/editor" />}` per the base-ui polymorphic-render pattern already used by `DropdownMenuTrigger` elsewhere). `components/editor/workspace/workspace-shell.tsx` (client) owns its own `isAiSidebarOpen` state and renders a self-contained shell — its own `h-12` top bar (project name left, `Share` button + `PanelRightOpen`/`PanelRightClose` AI toggle right) plus a `h-[calc(100vh-3rem)]` flex row (canvas placeholder filling remaining space, right AI sidebar placeholder sliding in as a plain conditional render, not yet animated) — deliberately separate from the global `EditorNavbar`/`EditorChrome` (which stays project-agnostic and unchanged, since it's shared by the project-list home page too). Reused the *existing* `ProjectSidebar` unmodified except for one addition: it now reads `usePathname()` to derive the current room id from `/editor/{id}` and passes `isActive` into `ProjectRow` (owned rows) / a conditional `bg-elevated` class (shared rows) to satisfy "current room highlighted in the sidebar." Verified via `tsc --noEmit` (had to run `npx next typegen` first — `PageProps<'/editor/[roomId]'>` isn't available until route types are generated), `eslint`, `next build`, and a temporary Playwright + `@clerk/testing` + `@clerk/backend` smoke test (script lived at repo root during the run only, for node module resolution; removed after, along with the three temporarily-installed packages) that covered: unauthenticated visit to `/editor/[roomId]` redirects to `/sign-in`; an owner creating a project via the real API then visiting its room sees the project name, Share button, canvas placeholder text, and can toggle the AI sidebar open (shows "AI Chat" heading); the sidebar row for the currently-open room carries the highlight class; a second throwaway Clerk user with no ownership/collaboration on that project sees `AccessDenied` (and its "Back to editor" link navigates to `/editor`); a nonexistent room id also shows `AccessDenied`; no console errors on reload. Both throwaway Clerk users and the test project were deleted at the end of the run.

- Post-08 bug fixes: three real bugs reported after 08 landed, all fixed.
  1. `AccessDenied`'s `Button render={<Link href="/editor" />}` rendered an `<a>` while Base UI's `Button` still assumed a native `<button>` (`nativeButton` defaults `true`), throwing a console error every render. Fixed with `nativeButton={false}`, confirmed against `node_modules/@base-ui/react/internals/types.d.ts`'s `NativeButtonProps` doc comment.
  2. "Stuck rendering after sign-in" and "create project doesn't redirect" were the same root cause: `app/editor/layout.tsx` `await`ed `auth()` + `currentUser()` + two Prisma `findMany` calls (owned/shared projects) at the top of the function body before returning any JSX. Per `node_modules/next/dist/docs/.../file-conventions/loading.md`: "If the layout accesses uncached or runtime data... navigation blocks until the layout finishes rendering" — no `loading.tsx` can paper over that, because `loading.js` wraps `page.js`, not the co-located `layout.js`. Every navigation into `/editor/*` (first one after sign-in, and every `router.push` afterward, including the post-create redirect) blocked on two live Postgres round-trips with zero visual feedback, reading as "stuck" until an unrelated hard refresh raced ahead of it. Fixed by never awaiting those two queries in the layout body — `ownedProjectsPromise`/`sharedProjectsPromise` are handed to `EditorChrome` unresolved, which renders `{children}` (the actual route) immediately and only suspends the sidebar: a new `components/editor/project-sidebar-loader.tsx` client component calls React 19's `use()` on both promises inside a `<Suspense>` in `editor-chrome.tsx`, falling back to `<ProjectSidebar ... isLoading />` (new prop, swaps "No projects yet" for "Loading projects…") until they resolve. `project-dialogs-context.tsx` no longer carries `ownedProjects`/`sharedProjects` at all — that was the reason `ProjectDialogsProvider` (which wraps `children`) was implicated in the blocking chain; it now only exposes the dialog-open functions, and `ProjectSidebar` takes the project arrays as direct props instead of from context. Also added `app/editor/[roomId]/loading.tsx` (this one *is* effective, since the async work lives in `page.tsx`, not a same-segment layout) so the create-dialog's `router.push` to the new room shows an instant "Loading project…" state instead of a silent gap while `getCurrentIdentity`/`getProjectForIdentity` run.
  3. Sidebar rows (both owned and shared) never navigated anywhere — `ProjectRow` rendered the project name as a plain `<span>`, and the shared-tab row was a plain `<div>`; only the owned row's `⋮` menu (rename/delete) did anything. Clicking a project name was always a no-op. Fixed by making the name a `next/link` `<Link href={`/editor/${project.id}`}>` (`flex-1 truncate`, sibling to the dropdown-trigger button so the menu click doesn't also navigate) for owned rows, and wrapping the whole row in a `Link` for shared rows (no menu to conflict with). Both call `onClose` on click to dismiss the sidebar overlay after navigating.
  - Also deleted `hooks/use-project-dialogs.ts` — a dead, untracked leftover from before 07-wire-editor-home's migration to `use-project-actions.ts` (progress tracker already claimed it was deleted, but it silently remained on disk referencing `lib/projects.ts`'s removed `mockProjects` export, breaking `tsc --noEmit`).
  - All three verified together via a temporary Playwright + `@clerk/backend` smoke test run against `next build && next start` (not `next dev`, to avoid Next's single-dev-server-per-project lock while a real dev server was running) on a throwaway port: sign-in landed on `/editor` with the home content visible immediately (not blocked on the sidebar's data), clicking a freshly-created project's sidebar row navigated to its room, and the create-dialog flow redirected to the new room's URL in ~250ms with no console errors. Script and temp packages removed after; `package.json`/lockfile unaffected (verified via `git status`).

- Post-08 bug fixes round 2: four more reported issues, all fixed.
  1. Newly created project didn't appear in the sidebar until a manual refresh. Cause: `submitCreate` in `hooks/use-project-actions.ts` only called `router.push` after `POST /api/projects`, never `router.refresh()` (unlike `submitRename`/`submitDelete`, which already did). Next's client Router Cache dedupes the shared `/editor` layout segment across sibling navigations, so `app/editor/layout.tsx` wasn't re-invoked for the new `/editor/{roomId}` URL and `ownedProjectsPromise` stayed the pre-create snapshot. Fixed by adding `router.refresh()` right after the push.
  2. Reported as "selecting a project in the sidebar doesn't highlight it" — verified via smoke test this was already working correctly (`ProjectRow`'s `isActive && "bg-elevated"` from the 08 session). The likely confusion: a project row's link auto-closes the sidebar overlay on click (`onNavigate={onClose}`), so the highlighted state is only visible if you reopen the sidebar afterward — reopening it does show the correct row highlighted. No code change.
  3. Sidebar covered the workspace's own top bar (hiding the project name) instead of pushing the canvas over, because `ProjectSidebar` is a `fixed` overlay per the original 02-editor-chrome spec ("floating overlay... doesn't push content"). Per explicit user feedback this is now push-not-overlay on desktop: `editor-chrome.tsx`'s `<main>` gets `lg:ml-72` (sidebar's width) when open, with a `transition-[margin-left]`. Left as overlay+backdrop on `<lg` (mobile) where 288px of shifted content would crowd the canvas — that half of the original design is unchanged.
  4. `WorkspaceShell`'s top bar showed the project name left-aligned next to the actions. Changed to the same three-`flex-1` left/center/right pattern `EditorNavbar` already uses, with the name in the centered slot.
  - All four verified via a temporary Playwright + `@clerk/backend` smoke test against `next build && next start` on a throwaway port (same reason as the prior round: avoid Next's single-dev-server-per-project lock while a real dev server was running): created a project via the dialog and confirmed its sidebar row appears without a refresh; clicked an existing row and confirmed only its row carries the bare `bg-elevated` class (not the `hover:bg-elevated` on others — an early test draft false-failed here by substring-matching the hover variant); confirmed `<main>`'s bounding box shifts right at desktop width when the sidebar is open and returns flush-left when closed; confirmed the workspace bar's project name is horizontally centered in the viewport. No console errors. Script and temp packages removed after; `package.json`/lockfile unaffected.

- Post-08 bug fixes round 3: two UX tweaks to `components/editor/project-sidebar.tsx`, both requested explicitly (overriding round 2's earlier choices).
  1. Clicking a project row no longer closes the sidebar — removed the `onClick={onClose}`/`onNavigate` wiring added in round 2. The sidebar now only closes via the navbar toggle, the `X` button, or (on `<lg`) tapping the backdrop.
  2. The active-row highlight was `bg-elevated`, the same token used for hover — too close in value to read as "selected." Swapped to the brand-accent pair already defined for this purpose: `bg-accent-dim` (row background) + `text-brand` (name text, plus `font-medium` for owned rows) instead of `bg-elevated`.

- 09-share-dialog: `lib/collaborators.ts` holds the shared `CollaboratorSummary` type (`id`/`email`/`name`/`imageUrl`/`createdAt`) and `EMAIL_PATTERN` — no runtime imports, so it's safe for both the API route and the client hook to import. `lib/clerk-users.ts` exports `getClerkUsersByEmail(emails)`, using `clerkClient()` from `@clerk/nextjs/server` and `client.users.getUserList({ emailAddress: emails })` (accepts up to 100 emails per call) to build an `email -> { name, imageUrl }` map; falls back to `null`/`null` per-field when Clerk has no user for that email (handled by the caller, not this function — it simply omits missing emails from the map). `app/api/projects/[projectId]/collaborators/route.ts`: `GET` uses `getCurrentIdentity`/`getProjectForIdentity` from `lib/project-access.ts` (owner OR collaborator membership check, same helper the workspace route already used) then lists+enriches; `POST` re-checks strict ownership itself (`project.ownerId !== userId` → `403`, matching the existing `[projectId]/route.ts` PATCH/DELETE convention rather than reusing the membership helper, since invite is owner-only) and returns `409` on a duplicate `[projectId, email]` pair. `app/api/projects/[projectId]/collaborators/[collaboratorId]/route.ts`'s `DELETE` is owner-only, and checks `collaborator.projectId === projectId` before deleting so one owner can't delete another project's collaborator row by id. `hooks/use-collaborators.ts` intentionally has no `isLoading` state variable — `collaborators` starts as `null` (not `[]`) and `isLoading` is derived as `enabled && collaborators === null`, because `eslint-plugin-react-hooks`'s new `set-state-in-effect` rule flags any setState call reachable synchronously (i.e., before the first `await`) from an effect's call stack, which an explicit `setIsLoading(true)` at the top of a fetch-triggering effect always is; deriving loading from "data is still null" sidesteps the pattern entirely instead of fighting the linter. `components/editor/dialogs/share-dialog.tsx` (client) takes `projectId`/`projectName`/`isOwner`, renders a copy-link button (`Copied!` for 2s via local state, `navigator.clipboard.writeText`), an invite form + per-row remove buttons gated on `isOwner`, and a `CollaboratorAvatar` that renders the Clerk `imageUrl` via a plain `<img>` (no `next/image`, since `next.config.ts` has no remote-image domain allowlist and Clerk's avatar host wasn't worth adding just for this) or an initials fallback circle when absent. Wired into `components/editor/workspace/workspace-shell.tsx`'s existing (previously inert) `Share` button; `isOwner` is computed once in `app/editor/[roomId]/page.tsx` (`project.ownerId === identity.userId`) and passed down — no new access-control helper needed since `getProjectForIdentity` already ran there. Verified via `tsc --noEmit`, `eslint`, `next build`, and a temporary Playwright + `@clerk/backend` smoke test (`playwright`/`@clerk/testing` installed with `--no-save`, both uninstalled after) against `next build && next start` on a throwaway port: signed in three throwaway Clerk users via the sign-in **ticket strategy** (`clerk.signInTokens.createSignInToken({ userId })` then, in-page, `window.Clerk.client.signIn.create({ strategy: "ticket", ticket })` + `window.Clerk.setActive(...)`) rather than filling the real sign-in form — filling the form hit an unrelated `/sign-in/client-trust` hang under `next start` that the ticket strategy bypasses entirely (matches the "ticket strategy" approach 07-wire-editor-home's notes already mentioned using, for the same reason). Confirmed: owner sees and can open the share dialog, copy-link shows `Copied!`, inviting a collaborator by email shows their Clerk-enriched display name (not just the email), re-inviting the same email surfaces the `409` as an inline error, a collaborator account sees the collaborator list with no invite form and no remove buttons, an outsider account gets `404` on `GET .../collaborators` and `403` on `POST .../collaborators` (ownership enforced server-side, not just hidden in the UI), owner removing a collaborator updates the list back to the empty state, and — separately — the previously-reported "deleting a project from the sidebar doesn't remove it without a manual refresh, and leaves the URL on the deleted project's page" bug (see fix below) no longer reproduces: deleting the currently-open project from its sidebar row redirects to `/editor` and the row disappears from the sidebar with no manual reload. Both Clerk test users and the throwaway server were cleaned up after.
- Bug fix (reported mid-09-share-dialog-session): deleting a project from the sidebar left it in the list until a manual refresh, and if it was the currently-open project, the URL never navigated away. Root cause: `hooks/use-project-actions.ts`'s `submitDelete` only called `router.refresh()` in the *non*-active-workspace branch; the active-workspace branch called `router.push("/editor")` alone. This is the same Router-Cache staleness class of bug as the "Post-08 bug fixes round 2" create-flow fix (`app/editor/layout.tsx`'s owned-projects promise living in a client-persisted `EditorChrome`/layout segment shared across sibling `/editor/*` routes) — a bare `push` to a sibling route doesn't guarantee that shared segment re-fetches. Fixed by always calling `router.refresh()` after the delete succeeds, in addition to the conditional `push`, mirroring `submitCreate`'s already-working push+refresh pattern. Verified as part of the 09-share-dialog smoke test above (see that entry's last sentence).
- Bug fix (reported after 09-share-dialog shipped): a newly-invited collaborator's "Shared" sidebar tab didn't show the project they'd just been given access to until they hard-refreshed the page — and refreshing also lost the sidebar's open/closed state, compounding the annoyance. Root cause: `ownedProjectsPromise`/`sharedProjectsPromise` are computed once per render of `app/editor/layout.tsx` and streamed down through `EditorChrome`. Every previous fix in this class (create/rename/delete) worked by calling `router.refresh()` after the *local* user's own mutation — but an invite is a mutation made by the *owner's* browser session, which the collaborator's session has no way to know about; their copy of the layout's promises was simply never invalidated by anything they did. Fixed in `components/editor/editor-chrome.tsx`: `EditorNavbar`'s sidebar toggle now calls `router.refresh()` whenever the sidebar transitions closed → open (not on close, and not via a `setState` updater — `router.refresh()` is a side effect and updater functions must stay pure), so opening the sidebar always re-fetches both project lists from Prisma regardless of who changed the underlying data. Verified via a temporary Playwright + `@clerk/backend` smoke test (same throwaway-package pattern as before): collaborator signs in and opens the sidebar's Shared tab *before* being invited (empty, as expected); owner invites them from a separate session; collaborator — same session, no page reload — opens the sidebar again and the project now appears. Confirms the fix works across the exact "already sitting on the app when someone shares with you" scenario reported.
- Bug fix (reported right after the fix above): the sidebar's open/closed state still didn't survive an actual hard page reload (F5) — the previous fix only addressed staleness of the sidebar's *contents*, not the fact that `isSidebarOpen` was plain `useState(false)` client state with no persistence, so a real reload always snapped it back to closed. Fixed by persisting it in a `sidebar-open` cookie (non-httpOnly, 1-year `max-age`, written via `document.cookie` on every open/close/toggle in `components/editor/editor-chrome.tsx`) and reading it server-side in `app/editor/layout.tsx` via `(await cookies()).get("sidebar-open")` to compute an `initialSidebarOpen` prop `EditorChrome`'s `useState` is seeded from — this avoids the alternative (client-only `localStorage` read in a `useEffect`) which would have caused a visible open/closed flash on every load since the server-rendered markup (and the `lg:ml-72` main-content margin) wouldn't match the client's restored value until after hydration. Verified via a temporary Playwright smoke test: fresh sign-in defaults to closed, opening then hard-reloading (`page.reload()`, not client navigation) keeps it open, closing then hard-reloading keeps it closed.
- Bug fix (reported immediately after the fix above): on a hard reload, the sidebar always defaulted to the "My Projects" tab even when the currently-open project (per the URL) was a *shared* one — a collaborator refreshing their own shared project's page would see the right project name in the workspace bar, but opening the sidebar landed them on the wrong tab with no indication their project was one click away on "Shared". `components/editor/project-sidebar.tsx`'s `Tabs` had a hardcoded `defaultValue="mine"`. Fixed by computing `defaultTab` from whether `currentRoomId` (already derived from `usePathname()` for the row-highlighting logic) matches an entry in `sharedProjects`, and passing that as `defaultValue` instead. Works correctly across reloads specifically because `ProjectSidebar` is a fresh mount every time the Suspense boundary swaps from its loading fallback to the real, data-populated instance (they're siblings under the same `<Suspense>`, not the same persisted instance) — `defaultValue` (uncontrolled) is exactly right here since `Tabs` isn't otherwise controlled. Note this only affects the initial mount: navigating client-side between an owned and a shared project without a reload won't retroactively flip the already-mounted tab, since that mount is preserved (not reset) across the `router.refresh()` calls from the earlier sidebar-open fix — same tradeoff already accepted for that fix, and not what was reported here. Verified via a temporary Playwright smoke test: owner shares a project, collaborator navigates directly to that project's URL, hard-reloads it, opens the sidebar — "Shared" tab is selected (`aria-selected="true"`) and the project row is visible without switching tabs.

## In Progress

- None yet.

## Next Up

- Build real canvas logic for `WorkspaceShell`'s center panel: Liveblocks room provider (room id = the `roomId`/`Project.id` already resolved and access-checked in `app/editor/[roomId]/page.tsx`) + React Flow canvas per `context/ui-context.md`'s node/edge/canvas conventions.

## Open Questions

- Add unresolved product or implementation questions here.

## Architecture Decisions

- globals.css defines shadcn's standard tokens (background/foreground/primary/etc.) plus custom semantic tokens (bg-base, bg-surface, bg-elevated, bg-subtle, border-surface-border, border-surface-border-subtle, text-copy-primary/secondary/muted/faint, text-brand, bg-accent-dim, bg-ai/text-ai-text, bg-error/success/warning) per context/ui-context.md, both pointing at the same dark hex palette. `<html>` carries a permanent `dark` class since the app has no light mode.
- This repo's Next.js (16.2.11) and Clerk (`@clerk/nextjs` 7.5.22, `@clerk/ui` 1.25.6) are both ahead of general training data — confirmed against `node_modules/next/dist/docs` and the installed packages' own `.d.ts` files rather than assumed. Notably: `middleware.ts` is renamed to `proxy.ts` (same runtime behavior, `next.config` matcher unchanged); Clerk's appearance API uses `theme:` (not the older `baseTheme:`) inside `appearance={{ theme, variables }}`.
- Route protection uses `createRouteMatcher` + `auth.protect()` in `proxy.ts`, per the auth feature spec. Clerk's own SDK logs a deprecation warning for `createRouteMatcher` in favor of resource-based (per-page/action) auth checks — still functional, not removed, but worth revisiting if this Clerk version bumps further.
- Fixed a pre-existing bug in globals.css: `--font-sans` was defined as `var(--font-sans)` (circular, self-referencing), so the `font-sans` Tailwind utility never actually resolved to Geist Sans anywhere in the app — it silently fell back to the browser default font. Now maps to `var(--font-geist-sans)` (and added `--font-mono: var(--font-geist-mono)` alongside it). This affects all pages, not just auth. Clerk's `appearance.variables.fontFamily`/`fontFamilyMono` were also pinned to the same Geist variables since Clerk's portal-rendered UI doesn't reliably inherit body font otherwise.
- Fixed a bug in `prisma.config.ts`: it only did `import "dotenv/config"`, which loads `.env` — but `DATABASE_URL` (and all other secrets) live in `.env.local` (Next.js's convention, confirmed by `next build`'s own "Environments: .env.local, .env" line). The Prisma CLI has no built-in awareness of `.env.local`, so `prisma migrate dev` failed with "datasource.url property is required" even though the app itself worked fine. Fixed by explicitly loading `.env.local` with `override: true` after the default `dotenv/config` import.
- Prisma 7.9.0's generated client (`prisma-client` provider, output already pointed at `app/generated/prisma` before this feature) accepts either `adapter` or `accelerateUrl` in its constructor — mutually exclusive at the type level. Accelerate no longer needs the separate `@prisma/extension-accelerate` package for basic connectivity in this version; only add that package later if caching (`cacheStrategy`) is needed.
- `proxy.ts`'s `isPublicRoute` matcher now includes `/api(.*)`, exempting all API routes from middleware-level `auth.protect()`. Discovered while building 06-project-apis: `auth.protect()` in `clerkMiddleware` 307-redirects unauthenticated requests to `/sign-in` (confirmed via curl, header `x-clerk-auth-reason: dev-browser-missing`) instead of returning `401` JSON, which the spec required. Clerk's own SDK already prints a deprecation warning for `createRouteMatcher` favoring resource-based (per-route) auth checks over middleware path-matching — this change follows that: every API route handler now calls `auth()` itself and returns `401`/`403` explicitly. Any future API route must do its own `auth()` check; it is no longer protected by the proxy.
- Project ids are now client-generated (`slugify(name)-{shortSuffix}`) and sent to `POST /api/projects`, which uses them as the literal `Project.id` (falling back to Prisma's `cuid()` default only if the client omits/mis-formats it). This is a deliberate divergence from 06-project-apis's "use the schema's existing ID strategy" note, which was guarding against sequential/auto-increment ids, not client-supplied ones. Reasoning: `architecture-context.md` states canvas snapshots live at `canvas/{projectId}.json` and that "Liveblocks room tokens are issued only after verifying project membership" — there's no separate `roomId` field anywhere in the schema, so the project id *is* the Liveblocks room id by construction. A human-readable, collision-resistant id (slug + suffix) makes that shared id useful as a room/URL identifier instead of an opaque cuid. If a future spec explicitly wants opaque server-generated ids instead, this is the code to revisit: `hooks/use-project-actions.ts`'s `roomId` computation and the `id` handling in `app/api/projects/route.ts`'s `POST`.
- Server-side project fetching for the sidebar lives in `app/editor/layout.tsx`, not `app/editor/page.tsx`, even though 07-wire-editor-home's spec says "the editor home page is a server component... fetch... pass both lists to the sidebar." The sidebar is rendered by the layout (it wraps every route under `/editor/*`, including the future `/editor/[projectId]` workspace route), not by the home page itself, so the layout is the component that actually needs the data. `app/editor/page.tsx` was left untouched.
- Shared projects are matched via `ProjectCollaborator.email` against the signed-in user's Clerk `primaryEmailAddress`, not a Clerk user id — `ProjectCollaborator` only stores `email` in the schema (collaborators are invited by email before they necessarily have an account). If a user's primary email changes, or a collaborator is invited under a non-primary email, they won't see the shared project — acceptable for now, worth revisiting if collaboration becomes a bigger feature.

## Session Notes

- Add context needed to resume work in the next session.
